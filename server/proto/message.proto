syntax = "proto3";

package chat;
option go_package = "server/pb";

message ChatMessage {
  // The user who sent the message.
  string user = 1; 

  // The content of the message.
  string message = 2; 
}
